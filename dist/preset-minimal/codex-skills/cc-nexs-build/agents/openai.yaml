interface:
  display_name: "/cc-nexs:build"
  short_description: "Run /cc-nexs:build explicitly"
  default_prompt: "Use $cc-nexs-build to run /cc-nexs:build."
policy:
  allow_implicit_invocation: false
