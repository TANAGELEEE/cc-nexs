interface:
  display_name: "/cc-nexs:release-base"
  short_description: "Run /cc-nexs:release-base explicitly"
  default_prompt: "Use $cc-nexs-release-base to run /cc-nexs:release-base."
policy:
  allow_implicit_invocation: false
