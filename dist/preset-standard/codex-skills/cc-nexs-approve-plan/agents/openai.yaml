interface:
  display_name: "/cc-nexs:approve-plan"
  short_description: "Run /cc-nexs:approve-plan explicitly"
  default_prompt: "Use $cc-nexs-approve-plan to run /cc-nexs:approve-plan."
policy:
  allow_implicit_invocation: false
