interface:
  display_name: "/cc-nexs:planner"
  short_description: "Run /cc-nexs:planner explicitly"
  default_prompt: "Use $cc-nexs-planner to run /cc-nexs:planner."
policy:
  allow_implicit_invocation: false
