interface:
  display_name: "/cc-nexs:verify"
  short_description: "Run /cc-nexs:verify explicitly"
  default_prompt: "Use $cc-nexs-verify to run /cc-nexs:verify."
policy:
  allow_implicit_invocation: false
