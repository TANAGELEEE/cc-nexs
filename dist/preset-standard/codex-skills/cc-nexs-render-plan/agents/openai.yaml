interface:
  display_name: "/cc-nexs:render-plan"
  short_description: "Run /cc-nexs:render-plan explicitly"
  default_prompt: "Use $cc-nexs-render-plan to run /cc-nexs:render-plan."
policy:
  allow_implicit_invocation: false
