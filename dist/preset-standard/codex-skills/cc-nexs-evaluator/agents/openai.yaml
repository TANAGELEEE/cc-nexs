interface:
  display_name: "/cc-nexs:evaluator"
  short_description: "Run /cc-nexs:evaluator explicitly"
  default_prompt: "Use $cc-nexs-evaluator to run /cc-nexs:evaluator."
policy:
  allow_implicit_invocation: false
