interface:
  display_name: "/cc-nexs:approve-release"
  short_description: "Run /cc-nexs:approve-release explicitly"
  default_prompt: "Use $cc-nexs-approve-release to run /cc-nexs:approve-release."
policy:
  allow_implicit_invocation: false
